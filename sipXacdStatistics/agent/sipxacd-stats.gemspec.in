 Gem::Specification.new do |spec|
    spec.platform = Gem::Platform::RUBY
    spec.authors = "sipfoundry"
    spec.summary = "@GEM_NAME@ SOAP web service for sipXacd statistics"
    spec.name = '@GEM_NAME@'
    spec.version = '@GEM_VER@'
    spec.requirements << 'none'
    spec.require_path = 'lib'
    spec.files = Dir.glob("lib/**/*")
    spec.description = <<EOF
  @GEM_NAME@ implements a SOAP based API for an statistics event analyzer for sipXacd.
EOF
end
