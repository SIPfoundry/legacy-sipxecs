/**
 * This package contains implementations for all providers defined in Openfire. Uses MongoDB backend.
 * Currently, each table in Openfire schema is replicated into a mongodb collection. In some cases, collections should
 * be converted into embedded documents, for easier querying.
 */
package org.sipfoundry.openfire.provider;

