include $(top_srcdir)/config/utility.am

cfgdir = $(SIPX_CONFDIR)/snom
dist_cfg_DATA = $(shell cd $(srcdir); find snom -type f)

snomm3dir = $(SIPX_CONFDIR)/snom-m3
dist_snomm3_DATA = $(shell cd $(srcdir); find snom-m3 -type f)
