# Log messages from remote clients so phones can log to the server
$ModLoad imudp # provides UDP syslog reception
$UDPServerRun 514

# The phonelogd starts this syslogd in a chroot jail, so the log file
# appears to be in the root directory.
*.*		/phones.log
