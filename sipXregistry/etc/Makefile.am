include $(top_srcdir)/config/utility.am

EXTRA_DIST = \
	$(cfdefaults_DATA:=.in)

cfdefaultsdir = \
	$(SIPX_CFDATA)/defaults

# part 1 is generated by sipxconfig
cfdefaults_DATA = \
	registrar-config.part2

cfinputsdir = $(SIPX_CFINPUTS)/plugin.d
dist_cfinputs_DATA = \
	sipxregistrar.cf

$(cfdefaults_DATA) : % : %.in Makefile
	@$(call SearchAndReplace, $<, $@)

CLEANFILES = $(cfdefaults_DATA)
