include $(top_srcdir)/config/utility.am

cfgdir = $(SIPX_CONFDIR)/karel
dist_cfg_DATA = $(shell cd $(srcdir); find karel -type f)

ip11xdir = $(SIPX_CONFDIR)/karel-ip11x
dist_ip11x_DATA = $(shell cd $(srcdir); find karel-ip11x -type f)
